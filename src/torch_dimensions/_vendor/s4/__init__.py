"""state-spaces/s4, standalone modules, redistributed verbatim (Apache-2.0).

See ``torch_dimensions._vendor`` for the verification scheme. Import
``torch_dimensions._vendor.s4.s4d`` / ``.s4`` directly — nothing is imported
here so that holding a reference to the package stays free of the einops,
numpy and scipy dependencies the modules themselves need.
"""
